---
import { requireAuth } from '../../../lib/auth';
import { getOrderByNumber, getPaymentsByOrderId } from '../../../lib/orders';

const redirect = requireAuth(Astro);
if (redirect) return redirect;

const { numero } = Astro.params;
const order = numero ? getOrderByNumber(numero) : null;
const payments = order?.id ? getPaymentsByOrderId(order.id) : [];

const deliveryLabel = order?.delivery_type === 'avion' ? 'EXPRESS (avion)' : 'ECONOMIQUE (bateau)';
const fmt = (n: number) => n.toLocaleString('fr-FR');
const dateStr = (d: string) => new Date(d).toLocaleDateString('fr-FR', { day: '2-digit', month: 'long', year: 'numeric' });

// Estimated delivery: 14 days for air, 45 days for sea
const deliveryDays = order?.delivery_type === 'avion' ? 14 : 45;
const estimatedDeliveryStr = order
  ? dateStr(new Date(new Date(order.created_at!).getTime() + deliveryDays * 24 * 60 * 60 * 1000).toISOString())
  : '';

const statusLabel: Record<string, string> = {
  en_attente: 'En attente',
  disponible: 'Disponible',
  recupere: 'Récupéré',
};
---

<!doctype html>
<html lang="fr">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Bon de commande {order?.order_number ?? 'introuvable'}</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: Arial, 'Helvetica Neue', Helvetica, sans-serif;
      font-size: 13px;
      color: #111;
      background: #f3f4f6;
    }

    /* Print action bar */
    .no-print {
      position: sticky;
      top: 0;
      z-index: 100;
      background: #1e40af;
      padding: 10px 24px;
      display: flex;
      align-items: center;
      gap: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    }
    .no-print button {
      background: #fff;
      color: #1e40af;
      border: none;
      border-radius: 6px;
      padding: 8px 18px;
      font-size: 14px;
      font-weight: 700;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 6px;
    }
    .no-print button:hover { background: #e0e7ff; }
    .no-print a {
      color: #bfdbfe;
      text-decoration: none;
      font-size: 13px;
      font-weight: 600;
    }
    .no-print a:hover { color: #fff; }
    .no-print .spacer { flex: 1; }
    .no-print .hint {
      color: #93c5fd;
      font-size: 12px;
    }

    /* A4 page wrapper */
    .page-wrapper {
      display: flex;
      justify-content: center;
      padding: 24px 16px 48px;
    }

    .page {
      background: #fff;
      width: 100%;
      max-width: 800px;
      min-height: 1100px;
      padding: 40px 48px;
      box-shadow: 0 4px 24px rgba(0,0,0,0.10);
      border-radius: 4px;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 6px;
    }
    .company-name {
      font-size: 30px;
      font-weight: 900;
      color: #1e40af;
      letter-spacing: -0.5px;
      line-height: 1;
    }
    .company-tagline {
      font-size: 11px;
      color: #6b7280;
      font-weight: 600;
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-top: 4px;
      text-align: center;
    }
    .company-contact {
      text-align: right;
      font-size: 11.5px;
      color: #374151;
      line-height: 1.7;
    }
    .company-contact strong {
      color: #1e40af;
      font-weight: 700;
    }

    /* Separator */
    .divider {
      height: 3px;
      background: linear-gradient(90deg, #1e40af 0%, #3b82f6 60%, #bfdbfe 100%);
      border-radius: 2px;
      margin: 14px 0;
    }

    /* Document title row */
    .doc-title-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 20px;
    }
    .doc-badge {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: #1e40af;
      color: #fff;
      font-size: 14px;
      font-weight: 800;
      letter-spacing: 1.5px;
      padding: 8px 18px;
      border-radius: 6px;
      text-transform: uppercase;
    }
    .order-number-badge {
      font-family: 'Courier New', Courier, monospace;
      font-size: 15px;
      font-weight: 700;
      color: #1e40af;
      background: #eff6ff;
      border: 1.5px solid #bfdbfe;
      padding: 5px 12px;
      border-radius: 5px;
      letter-spacing: 0.5px;
    }
    .doc-date {
      font-size: 12px;
      color: #6b7280;
      text-align: right;
    }
    .doc-date strong {
      display: block;
      font-size: 13.5px;
      color: #111;
    }

    /* Info grid */
    .info-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
      margin-bottom: 22px;
    }
    .info-box {
      border: 1px solid #e5e7eb;
      border-radius: 6px;
      overflow: hidden;
    }
    .info-box-header {
      background: #f8fafc;
      border-bottom: 1px solid #e5e7eb;
      padding: 7px 14px;
      font-size: 10px;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 1.2px;
      color: #6b7280;
    }
    .info-box-body {
      padding: 12px 14px;
      line-height: 1.8;
    }
    .info-box-body .label {
      font-size: 10.5px;
      color: #9ca3af;
      font-weight: 600;
      text-transform: uppercase;
    }
    .info-box-body .value {
      font-size: 13px;
      color: #111;
      font-weight: 600;
    }
    .info-box-body .value.big {
      font-size: 15px;
      font-weight: 700;
      color: #1e40af;
      text-transform: uppercase;
    }
    .delivery-badge {
      display: inline-block;
      padding: 3px 10px;
      border-radius: 20px;
      font-size: 11px;
      font-weight: 700;
    }
    .delivery-avion {
      background: #eff6ff;
      color: #1e40af;
      border: 1px solid #bfdbfe;
    }
    .delivery-bateau {
      background: #f0fdf4;
      color: #166534;
      border: 1px solid #bbf7d0;
    }
    .status-badge {
      display: inline-block;
      padding: 3px 10px;
      border-radius: 20px;
      font-size: 11px;
      font-weight: 700;
      background: #fefce8;
      color: #854d0e;
      border: 1px solid #fde68a;
    }
    .status-disponible {
      background: #f0fdf4;
      color: #166534;
      border-color: #bbf7d0;
    }
    .status-recupere {
      background: #f9fafb;
      color: #4b5563;
      border-color: #e5e7eb;
    }

    /* Products table */
    .section-title {
      font-size: 11px;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 1.2px;
      color: #6b7280;
      margin-bottom: 8px;
      padding-bottom: 4px;
      border-bottom: 1px solid #e5e7eb;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
      font-size: 12.5px;
    }
    thead tr {
      background: #1e40af;
      color: #fff;
    }
    thead th {
      padding: 9px 12px;
      text-align: left;
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.8px;
    }
    thead th:last-child, thead th:nth-child(3) { text-align: right; }
    thead th:nth-child(1) { text-align: center; width: 44px; }
    tbody tr:nth-child(even) { background: #f8fafc; }
    tbody tr:nth-child(odd) { background: #fff; }
    tbody td {
      padding: 9px 12px;
      border-bottom: 1px solid #f1f5f9;
      vertical-align: top;
      color: #1f2937;
    }
    tbody td:first-child { text-align: center; color: #6b7280; font-weight: 600; }
    tbody td:nth-child(3), tbody td:last-child { text-align: right; font-weight: 600; color: #111; }
    tfoot tr { background: #eff6ff; }
    tfoot td {
      padding: 9px 12px;
      font-weight: 700;
      color: #1e40af;
      text-align: right;
      font-size: 13px;
      border-top: 2px solid #bfdbfe;
    }
    tfoot td:first-child { text-align: center; color: #1e40af; }
    tfoot td:nth-child(2) { text-align: left; }
    .product-name { font-weight: 600; color: #111; }
    .product-condition {
      font-size: 10.5px;
      color: #9ca3af;
      margin-top: 1px;
    }

    /* Financial summary */
    .summary-wrapper {
      display: flex;
      justify-content: flex-end;
      margin-bottom: 20px;
    }
    .summary-box {
      min-width: 280px;
      max-width: 340px;
      border: 1.5px solid #e5e7eb;
      border-radius: 8px;
      overflow: hidden;
    }
    .summary-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 8px 16px;
      border-bottom: 1px solid #f1f5f9;
      font-size: 12.5px;
    }
    .summary-row:last-child { border-bottom: none; }
    .summary-label { color: #6b7280; font-weight: 500; }
    .summary-value { font-weight: 700; color: #111; }
    .summary-row.total-row {
      background: #f8fafc;
      font-size: 13.5px;
    }
    .summary-row.total-row .summary-label { color: #374151; font-weight: 700; }
    .summary-row.paid-row .summary-value { color: #059669; }
    .summary-row.remaining-positive .summary-value { color: #dc2626; }
    .summary-row.remaining-zero .summary-value { color: #059669; }
    .summary-row.remaining-row {
      background: #fef2f2;
    }
    .summary-row.remaining-zero-row {
      background: #f0fdf4;
    }

    /* Payments history */
    .payments-section {
      margin-bottom: 20px;
    }
    .payments-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 12px;
    }
    .payments-table th {
      background: #f8fafc;
      padding: 7px 12px;
      text-align: left;
      font-size: 10.5px;
      font-weight: 700;
      color: #6b7280;
      text-transform: uppercase;
      letter-spacing: 0.8px;
      border-bottom: 1px solid #e5e7eb;
    }
    .payments-table td {
      padding: 7px 12px;
      border-bottom: 1px solid #f1f5f9;
      color: #374151;
    }
    .payments-table td.amount {
      font-weight: 700;
      color: #059669;
      text-align: right;
    }

    /* Conditions */
    .conditions {
      background: #f8fafc;
      border: 1px solid #e5e7eb;
      border-radius: 6px;
      padding: 12px 16px;
      margin-bottom: 20px;
    }
    .conditions-title {
      font-size: 10px;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 1.5px;
      color: #6b7280;
      margin-bottom: 7px;
    }
    .conditions ol {
      padding-left: 16px;
      font-size: 11px;
      color: #4b5563;
      line-height: 1.6;
    }
    .conditions ol li { margin-bottom: 2px; }
    .conditions p {
      font-size: 11px;
      color: #4b5563;
      margin-top: 5px;
      font-style: italic;
    }

    /* Footer */
    .footer {
      text-align: center;
      padding-top: 16px;
      border-top: 1px solid #e5e7eb;
      color: #9ca3af;
      font-size: 11.5px;
    }
    .footer strong {
      display: block;
      font-size: 13px;
      color: #374151;
      font-weight: 700;
      margin-bottom: 3px;
    }

    /* Error page */
    .error-page {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 300px;
      gap: 12px;
      color: #6b7280;
    }
    .error-page h2 {
      font-size: 20px;
      color: #374151;
      font-weight: 700;
    }

    /* ===== BACK PAGE (conditions) ===== */
    .page-back {
      background: #fff;
      width: 100%;
      max-width: 800px;
      min-height: 1100px;
      padding: 40px 48px;
      box-shadow: 0 4px 24px rgba(0,0,0,0.10);
      border-radius: 4px;
      margin-top: 32px;
      display: flex;
      flex-direction: column;
    }
    .back-title {
      font-size: 13px;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 2px;
      color: #1e40af;
      text-align: center;
      margin-bottom: 6px;
      padding-bottom: 10px;
      border-bottom: 2px solid #1e40af;
    }
    .back-subtitle {
      font-size: 10px;
      color: #9ca3af;
      text-align: center;
      margin-bottom: 28px;
      font-style: italic;
    }
    .conditions-back {
      flex: 1;
    }
    .conditions-back ol {
      padding-left: 20px;
      font-size: 12.5px;
      color: #374151;
      line-height: 1.9;
      counter-reset: item;
    }
    .conditions-back ol li {
      margin-bottom: 12px;
      padding-left: 4px;
    }
    .conditions-back ol li::marker {
      font-weight: 700;
      color: #1e40af;
    }
    .conditions-back .privacy {
      margin-top: 24px;
      padding: 12px 16px;
      background: #eff6ff;
      border-left: 3px solid #3b82f6;
      border-radius: 0 6px 6px 0;
      font-size: 11.5px;
      color: #4b5563;
      font-style: italic;
    }
    .back-footer {
      text-align: center;
      padding-top: 20px;
      border-top: 1px solid #e5e7eb;
      color: #9ca3af;
      font-size: 11px;
      margin-top: auto;
    }
    .back-footer strong {
      display: block;
      font-size: 12px;
      color: #374151;
      font-weight: 700;
      margin-bottom: 3px;
    }
    .page-label {
      text-align: center;
      font-size: 11px;
      color: #9ca3af;
      font-weight: 600;
      letter-spacing: 1px;
      text-transform: uppercase;
      margin: 16px 0 8px;
    }

    /* ===== PRINT STYLES ===== */
    @media print {
      .no-print { display: none !important; }

      body {
        background: #fff;
        font-size: 11px;
      }

      .page-wrapper { padding: 0; }
      .page-label { display: none; }

      .page, .page-back {
        max-width: 100%;
        width: 100%;
        min-height: auto;
        box-shadow: none;
        padding: 15mm 18mm;
        border-radius: 0;
        margin: 0;
      }

      .page-back {
        page-break-before: always;
      }

      .company-name { font-size: 22px; }
      table { font-size: 10.5px; }
      thead th { font-size: 9.5px; }
      .conditions-back ol { font-size: 11.5px; }

      @page {
        size: A4;
        margin: 0;
      }
    }
  </style>
</head>
<body>

  <!-- Print action bar (hidden on print) -->
  <div class="no-print">
    <button onclick="window.print()">
      <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z"/>
      </svg>
      Imprimer
    </button>
    <a href="/admin/orders">← Retour aux commandes</a>
    <span class="spacer"></span>
    {order && <span class="hint">Commande {order.order_number}</span>}
  </div>

  <div class="page-wrapper">
    <div class="page">

      {!order ? (
        <div class="error-page">
          <svg width="48" height="48" fill="none" stroke="#d1d5db" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
          </svg>
          <h2>Commande introuvable</h2>
          <p>Le numéro de commande <strong>{numero}</strong> n'existe pas dans le système.</p>
          <a href="/admin/orders" style="margin-top:8px; color:#1e40af; font-weight:600;">← Retour aux commandes</a>
        </div>
      ) : (
        <>
          <!-- Header -->
          <div class="header">
            <div>
              <div class="company-name">TOGOLESE</div>
              <div class="company-tagline">Vente et distribution</div>
            </div>
            <div class="company-contact">
              <div>Kégué derrière l'hôtel O'Balkan, Lomé - TOGO</div>
              <div><strong>Tél :</strong> +228 90 22 64 91</div>
              <div><strong>Email :</strong> Contact@togolesegroup.com</div>
            </div>
          </div>

          <div class="divider"></div>

          <!-- Document title row -->
          <div class="doc-title-row">
            <div style="display:flex; flex-direction:column; align-items:center; gap:10px;">
              <span class="doc-badge">Bon de commande</span>
              <span class="order-number-badge">{order.order_number}</span>
            </div>
            <div class="doc-date">
              <span style="font-size:10.5px; color:#9ca3af; text-transform:uppercase; letter-spacing:0.8px;">Date d'émission</span>
              <strong>{dateStr(order.created_at!)}</strong>
            </div>
          </div>

          <!-- Info grid: client | livraison -->
          <div class="info-grid">
            <div class="info-box">
              <div class="info-box-header">Informations client</div>
              <div class="info-box-body">
                <div class="label">Nom</div>
                <div class="value big">{order.client_name}</div>
                <div style="margin-top:8px;">
                  <div class="label">Téléphone</div>
                  <div class="value">{order.client_phone}</div>
                </div>
                {order.notes && (
                  <div style="margin-top:8px;">
                    <div class="label">Notes</div>
                    <div class="value" style="font-size:12px; font-weight:500; color:#4b5563;">{order.notes}</div>
                  </div>
                )}
              </div>
            </div>
            <div class="info-box">
              <div class="info-box-header">Livraison &amp; Statut</div>
              <div class="info-box-body">
                <div class="label">Mode de livraison</div>
                <div class="value" style="margin-top:2px;">
                  <span class={`delivery-badge ${order.delivery_type === 'avion' ? 'delivery-avion' : 'delivery-bateau'}`}>
                    {order.delivery_type === 'avion' ? '✈️ EXPRESS (avion)' : '🚢 ECONOMIQUE (bateau)'}
                  </span>
                </div>
                <div style="margin-top:10px;">
                  <div class="label">Livraison prévue</div>
                  <div class="value" style="font-size:13px; font-weight:700; color:#1e40af;">
                    {estimatedDeliveryStr}
                  </div>
                </div>
                <div style="margin-top:10px;">
                  <div class="label">Statut commande</div>
                  <div class="value" style="margin-top:2px;">
                    <span class={`status-badge ${order.status === 'disponible' ? 'status-disponible' : order.status === 'recupere' ? 'status-recupere' : ''}`}>
                      {statusLabel[order.status ?? 'en_attente']}
                    </span>
                  </div>
                </div>
                {order.created_by && (
                  <div style="margin-top:10px;">
                    <div class="label">Créé par</div>
                    <div class="value" style="font-size:12px;">{order.created_by}</div>
                  </div>
                )}
              </div>
            </div>
          </div>

          <!-- Products table -->
          <div class="section-title">Détail des produits</div>
          <table>
            <thead>
              <tr>
                <th>Qté</th>
                <th>Description / Produit</th>
                <th>Prix unitaire</th>
                <th>Total</th>
              </tr>
            </thead>
            <tbody>
              {(order.products ?? []).length === 0 ? (
                <tr>
                  <td colspan="4" style="text-align:center; color:#9ca3af; font-style:italic; padding:20px;">
                    Aucun produit enregistré
                  </td>
                </tr>
              ) : (
                (order.products ?? []).map((product) => (
                  <tr>
                    <td>{product.quantity}</td>
                    <td>
                      <div class="product-name">{product.name}</div>
                      {product.condition && <div class="product-condition">{product.condition}</div>}
                    </td>
                    <td>{fmt(product.price)} FCFA</td>
                    <td>{fmt(product.price * product.quantity)} FCFA</td>
                  </tr>
                ))
              )}
            </tbody>
            {(order.products ?? []).length > 0 && (
              <tfoot>
                <tr>
                  <td>{(order.products ?? []).reduce((s, p) => s + p.quantity, 0)}</td>
                  <td>TOTAL</td>
                  <td></td>
                  <td>{fmt(order.total_amount)} FCFA</td>
                </tr>
              </tfoot>
            )}
          </table>

          <!-- Financial summary -->
          <div class="summary-wrapper">
            <div class="summary-box">
              <div class="summary-row total-row">
                <span class="summary-label">Montant total</span>
                <span class="summary-value">{fmt(order.total_amount)} FCFA</span>
              </div>
              <div class="summary-row paid-row">
                <span class="summary-label">Acompte versé</span>
                <span class="summary-value">{fmt(order.deposit)} FCFA</span>
              </div>
              <div class={`summary-row ${order.remaining_balance > 0 ? 'remaining-row remaining-positive' : 'remaining-zero-row remaining-zero'}`}>
                <span class="summary-label" style="font-weight:700;">Reste à payer</span>
                <span class="summary-value" style="font-size:15px;">
                  {fmt(order.remaining_balance)} FCFA
                </span>
              </div>
            </div>
          </div>

          <!-- Payments history (shown only if there are payments) -->
          {payments.length > 0 && (
            <div class="payments-section">
              <div class="section-title">Historique des versements</div>
              <table class="payments-table">
                <thead>
                  <tr>
                    <th>#</th>
                    <th>Date</th>
                    <th>Moyen de paiement</th>
                    <th>Enregistré par</th>
                    <th style="text-align:right;">Montant</th>
                  </tr>
                </thead>
                <tbody>
                  {payments.map((payment, i) => (
                    <tr>
                      <td style="color:#9ca3af; font-size:11px;">{i + 1}</td>
                      <td>{payment.created_at ? dateStr(payment.created_at) : '—'}</td>
                      <td>{payment.payment_method || '—'}</td>
                      <td>{payment.performed_by || '—'}</td>
                      <td class="amount">{fmt(payment.amount)} FCFA</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          )}

          <!-- Footer recto -->
          <div class="footer">
            <strong>Merci pour votre confiance !</strong>
            TOGOLESE &mdash; Kégué derrière l'hôtel O'Balkan, Lomé - TOGO &mdash; Contact@togolesegroup.com
          </div>
        </>
      )}

    </div>

    {order && (
      <>
        <p class="page-label">— Verso : Conditions générales —</p>

        <!-- Back page: conditions générales -->
        <div class="page-back">
          <div class="back-title">Conditions Générales de Vente</div>
          <div class="back-subtitle">TOGOLESE — Vente et distribution — Lomé, TOGO</div>

          <div class="conditions-back">
            <ol>
              <li>Acompte de 70% exigé à la commande. Le solde (30%) est dû à la livraison avant remise des articles.</li>
              <li>Une fois la commande passée et l'acompte versé, aucun remboursement n'est possible.</li>
              <li>Délai de réclamation : 1 mois à compter de la date de livraison. Passé ce délai, aucune réclamation ne sera prise en compte.</li>
              <li>Tout produit est vérifié et testé en présence du client lors de la remise de commande. Aucune réclamation relative à l'état du produit ne sera acceptée après cette vérification contradictoire.</li>
              <li>TOGOLESE ne peut être tenu responsable des retards de livraison involontaires liés aux transporteurs, aux douanes ou à tout événement indépendant de sa volonté.</li>
              <li>Les produits bénéficient d'une garantie constructeur selon les termes définis par le fabricant. Tout défaut constaté après réception doit être signalé dans le délai de réclamation. Aucune garantie ne couvre les dommages causés par une mauvaise utilisation, une chute, une infiltration d'eau ou une modification non autorisée.</li>
              <li>Le client s'engage à fournir des informations exactes lors de la commande. TOGOLESE ne saurait être tenu responsable des erreurs de livraison dues à des informations incorrectes.</li>
              <li>Tout litige relatif à la présente commande sera soumis à la juridiction compétente de Lomé, TOGO.</li>
            </ol>

            <div class="privacy">
              <strong>Protection des données personnelles :</strong> Les informations collectées lors de votre commande (nom, téléphone) sont utilisées exclusivement pour le traitement et le suivi de votre commande. Elles ne sont ni vendues ni transmises à des tiers.
            </div>
          </div>

          <div class="back-footer">
            <strong>TOGOLESE</strong>
            Kégué derrière l'hôtel O'Balkan, Lomé - TOGO &mdash; Tél : +228 90 22 64 91 &mdash; Contact@togolesegroup.com
          </div>
        </div>
      </>
    )}

  </div>

</body>
</html>
